# Copy this file to .env.local and fill in your values

# Base URL of the backend API. Falls back to http://127.0.0.1:8000 for local dev
# if unset, so this only needs to be set for deployed environments.
NEXT_PUBLIC_API_URL=https://demo-leadtodeal-api.example.com
